
spring:
  application:
    name: springboot-sql
  datasource:
    driver-class-name: org.postgresql.Driver
    password: 123456
    url: jdbc:postgresql://localhost:5432/springboot
    username: weilinxi
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8080

